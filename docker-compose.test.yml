services:
  fuel-cost-config-test-db:
    image: postgres:16
    environment:
      POSTGRES_DB: fuel_cost_config_tests
      POSTGRES_USER: fuel_cost_config
      POSTGRES_PASSWORD: fuel_cost_config
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./persistence/fuel-cost-config-test-db/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fuel_cost_config -d fuel_cost_config_tests"]
      interval: 5s
      timeout: 5s
      retries: 10

  fuel-cost-config-tests:
    build: ./local_services/fuel_cost_config
    command: pytest tests/ -v
    depends_on:
      fuel-cost-config-test-db:
        condition: service_healthy
      casdoor:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://fuel_cost_config:fuel_cost_config@fuel-cost-config-test-db/fuel_cost_config_tests
      CASDOOR_JWKS_URL: http://casdoor:8000/.well-known/jwks
      CASDOOR_ISSUER: http://localhost:8000
      CASDOOR_URL: http://casdoor:8000
    profiles:
      - test

  driver-tariff-config-test-db:
    image: postgres:16
    environment:
      POSTGRES_DB: driver_tariff_config_tests
      POSTGRES_USER: driver_tariff_config
      POSTGRES_PASSWORD: driver_tariff_config
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./persistence/driver-tariff-config-test-db/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U driver_tariff_config -d driver_tariff_config_tests"]
      interval: 5s
      timeout: 5s
      retries: 10

  driver-tariff-config-tests:
    build: ./local_services/driver_tariff_config
    command: pytest tests/ -v
    depends_on:
      driver-tariff-config-test-db:
        condition: service_healthy
      casdoor:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://driver_tariff_config:driver_tariff_config@driver-tariff-config-test-db/driver_tariff_config_tests
      CASDOOR_JWKS_URL: http://casdoor:8000/.well-known/jwks
      CASDOOR_ISSUER: http://localhost:8000
      CASDOOR_URL: http://casdoor:8000
    profiles:
      - test

  base-margin-config-test-db:
    image: postgres:16
    environment:
      POSTGRES_DB: base_margin_config_tests
      POSTGRES_USER: base_margin_config
      POSTGRES_PASSWORD: base_margin_config
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./persistence/base-margin-config-test-db/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U base_margin_config -d base_margin_config_tests"]
      interval: 5s
      timeout: 5s
      retries: 10

  base-margin-config-tests:
    build: ./local_services/base_margin_config
    command: pytest tests/ -v
    depends_on:
      base-margin-config-test-db:
        condition: service_healthy
      casdoor:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://base_margin_config:base_margin_config@base-margin-config-test-db/base_margin_config_tests
      CASDOOR_JWKS_URL: http://casdoor:8000/.well-known/jwks
      CASDOOR_ISSUER: http://localhost:8000
      CASDOOR_URL: http://casdoor:8000
    profiles:
      - test

  lead-time-config-test-db:
    image: postgres:16
    environment:
      POSTGRES_DB: lead_time_config_tests
      POSTGRES_USER: lead_time_config
      POSTGRES_PASSWORD: lead_time_config
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./persistence/lead-time-config-test-db/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U lead_time_config -d lead_time_config_tests"]
      interval: 5s
      timeout: 5s
      retries: 10

  lead-time-config-tests:
    build: ./local_services/lead_time_config
    command: pytest tests/ -v
    depends_on:
      lead-time-config-test-db:
        condition: service_healthy
      casdoor:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://lead_time_config:lead_time_config@lead-time-config-test-db/lead_time_config_tests
      CASDOOR_JWKS_URL: http://casdoor:8000/.well-known/jwks
      CASDOOR_ISSUER: http://localhost:8000
      CASDOOR_URL: http://casdoor:8000
    profiles:
      - test

  costing-engine-test-db:
    image: postgres:16
    environment:
      POSTGRES_DB: costing_engine_tests
      POSTGRES_USER: costing_engine
      POSTGRES_PASSWORD: costing_engine
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./persistence/costing-engine-test-db/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U costing_engine -d costing_engine_tests"]
      interval: 5s
      timeout: 5s
      retries: 10

  costing-engine-tests:
    build: ./local_services/costing_engine
    command: pytest tests/ -v
    depends_on:
      costing-engine-test-db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://costing_engine:costing_engine@costing-engine-test-db/costing_engine_tests
    profiles:
      - test
